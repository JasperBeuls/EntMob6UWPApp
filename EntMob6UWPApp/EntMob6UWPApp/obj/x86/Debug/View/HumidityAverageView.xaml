<Page
    x:Class="EntMob6UWPApp.View.HumidityAverageView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EntMob6UWPApp.View"
    xmlns:converters="using:EntMob6UWPApp.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding Source={StaticResource mainViewModelLocator},Path=HumidityAverageViewModel}">

    <UserControl.Resources>
        <converters:DateConverter x:Key="localDateConverter"></converters:DateConverter>
        <converters:DoubleConverter x:Key="localDoubleConverter"></converters:DoubleConverter>
    </UserControl.Resources>
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Viewbox Stretch="Uniform">


            <RelativePanel >
                <StackPanel x:ConnectionId='1' Name="ButtonStackPanel" Orientation="Horizontal" RelativePanel.AlignTopWithPanel="True" >
                    <Button x:ConnectionId='6' Name="OverviewPageButton" Content="Overview"  Margin="8" Command="{Binding OverviewCommand}"></Button>
                    <Button x:ConnectionId='7' Name="HistoryPageButton"  Content="Humidity"  Margin="8" ></Button>
                </StackPanel>
                



                    <Grid RelativePanel.Below="ButtonStackPanel">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"></ColumnDefinition>
                            <ColumnDefinition Width="*"></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                        <StackPanel  Orientation="Vertical"  Grid.Column="0"  >
                        <Button x:ConnectionId='2' Name="Minute"  Content="Minute"  Margin="8" Command="{Binding MinuteCommand}" MinWidth="75"></Button>
                        <Button x:ConnectionId='3' Name="Hour"  Content="Hour"  Margin="8" Command="{Binding HourCommand}" MinWidth="75"></Button>
                        <Button x:ConnectionId='4' Name="Day"  Content="Day"  Margin="8" Command="{Binding DayCommand}" MinWidth="75"></Button>
                        <Button x:ConnectionId='5' Name="Month"  Content="Month"  Margin="8" Command="{Binding MonthCommand}" MinWidth="75"></Button>
                    </StackPanel>
                        <ListView ItemsSource="{Binding HumidityAverages, Mode=OneWay}" Grid.Column="1">
                            <ListView.ItemTemplate>
                                <DataTemplate>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"></ColumnDefinition>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"></RowDefinition>
                                    </Grid.RowDefinitions>

                                    <StackPanel Orientation="Horizontal" Margin="11">
                                        <TextBlock Text="Average percentage: " ></TextBlock>
                                        <TextBlock Text="{Binding AvPer, Converter={StaticResource localDoubleConverter}}" Grid.Column="1"></TextBlock>

                                        <TextBlock Text=" Date: " ></TextBlock>
                                        <TextBlock Text="{Binding Date, Converter={StaticResource localDateConverter}}" Grid.Column="2"></TextBlock>
                                    </StackPanel>
                                </Grid>
                                    
                            </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>

                    </Grid>
                          
                
         
            </RelativePanel></Viewbox>
            
    </Grid>
</Page>

